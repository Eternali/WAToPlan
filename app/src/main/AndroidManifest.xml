<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chipthink.eternali.watoplan">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppThemeLight">
        <activity android:name="com.chipthink.eternali.watoplan.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.chipthink.eternali.watoplan.EditActivity" />
        <activity android:name="com.chipthink.eternali.watoplan.SettingsActivity" />
        <activity android:name="com.chipthink.eternali.watoplan.views.dialog.EditTypeActivity"
            android:theme="@style/edit_type_dialog_style"
            android:excludeFromRecents="true" />
    </application>

</manifest>